# Contiguous RT-Schedule Export Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Rewrite AIDIA's Thermo CSV export so the RT schedule tiles `[acquisition_start, acquisition_end]` with zero gaps, overlaps, or voids, and emits the native `t start (min)`/`t stop (min)` format matching `mass_list_example.csv`.

**Architecture:** Export-only change. A new internal helper `.compute_contiguous_rt_schedule()` derives a contiguous RT boundary array `B` from segment midpoints — edges clamped to user-supplied acquisition bounds, interior breaks set to the midpoint of adjacent segments' `rt_end`/`rt_start`, the whole array rounded once at 2 decimals. `export_windows_to_csv()` maps each window to its segment's `[B[j], B[j+1]]` and writes the new columns. Optimization, statistics, plotting, and S3 classes are untouched — the helper reads only the existing `windows$rt_start`/`rt_end` columns and never assumes `rt_group` contiguity.

**Tech Stack:** R package (`aidia`), dplyr, testthat (`devtools::test()`), roxygen2. Spec: `docs/superpowers/specs/2026-06-12-contiguous-rt-schedule-export-design.md`. Branch: `feature/contiguous-rt-export`.

---

## Background the engineer needs

- **What a "segment" is:** A distinct `(rt_start, rt_end)` pair in `optimized_windows$windows`. Each RT bin produces several m/z windows that all share the same `(rt_start, rt_end)`. The export must give every window in a segment the same `t start`/`t stop`.
- **Why midpoints, not `rt_breaks`:** `windows$rt_start`/`rt_end` are the *precursor-measured* RT span of each segment (clipped to where precursors actually elute), so adjacent segments are **not** contiguous — there are gaps. Putting the boundary at the midpoint of `segs[j].rt_end` and `segs[j+1].rt_start` closes the gap and is robust to empty/sparse interior bins (an empty band is simply absorbed by its two neighbors). We deliberately avoid indexing into a stored `rt_breaks`/`rt_group` array because the current pipeline (`mz_optimization.R::make_mz_range_row`) mixes positional and value-based `rt_group` access, silently assuming `rt_group` runs `1..n_bins` with no gaps. The new export must not inherit that fragility.
- **Round-once is what guarantees contiguity:** Adjacent segments share the *same* `B[j+1]` value, so `t_stop[j] == t_start[j+1]` exactly — at any rounding precision. The old export rounded `RT Time` (center) and `Window` (width) independently, so the shared boundary was reconstructed from a sum/difference of two separately-rounded numbers and drifted. Rounding the boundary array `B` exactly once removes that class of bug.
- **The window's internal `rt_start`/`rt_end` stay as-is** — plots and statistics still use the precursor-measured span. Only the CSV output uses `B`.
- **dplyr is fully imported** (`import(dplyr)` in `NAMESPACE`), so `distinct()`, `arrange()`, `mutate()` are available unqualified, matching the existing style in `R/export_methods.R`.
- **Internal helpers are testable:** `devtools::test()` runs `devtools::load_all()` first, so a non-exported, dot-prefixed function like `.compute_contiguous_rt_schedule()` is directly callable from test code inside the package namespace.

## File structure

| File | Responsibility | Change |
|---|---|---|
| `R/export_methods.R` | Thermo CSV export + the new boundary helper | New helper `.compute_contiguous_rt_schedule()`; rewrite RT columns of `export_windows_to_csv()`; `Adduct="(no adduct)"`; two new args |
| `tests/testthat/test-export-contiguous-rt.R` | All new export tests | Create |
| `main.R` | Pipeline entry point | Add `acquisition_start_min`/`acquisition_end_min` params, pass to export |
| `inst/shiny_app/ui_step3_results.R` | Step 3 download UI | Add "Run Length (min)" numeric input |
| `inst/shiny_app/server_downloads.R` | Download handlers | Thread acquisition end into the two `export_windows_to_csv()` call sites; refresh preview text |
| `man/export_windows_to_csv.Rd` | Generated docs | Regenerated by `devtools::document()` |
| `CLAUDE.md`, `docs/domain-knowledge.md` | Format docs | Update the 8-column description |

---

## Task 1: Contiguous RT boundary helper

The riskiest, most reusable piece. Build and unit-test it in isolation before wiring it into the export.

**Files:**
- Modify: `R/export_methods.R` (add helper above `export_windows_to_csv`, after the `calculate_loop_n` block, i.e. around line 44)
- Test: `tests/testthat/test-export-contiguous-rt.R` (create)

- [ ] **Step 1: Write the failing test**

Create `tests/testthat/test-export-contiguous-rt.R` with:

```r
# test-export-contiguous-rt.R
#
# Contiguous RT-schedule export (spec 2026-06-12):
# export must tile [acquisition_start, acquisition_end] with zero gap/overlap/void
# and emit the native `t start (min)` / `t stop (min)` format.

# --- fixtures -----------------------------------------------------------------

# 3 RT segments, 2 m/z windows each.
# seg1 rt 10-18, seg2 rt 20-28, seg3 rt 30-40.
# midpoints: (18+20)/2 = 19, (28+30)/2 = 29.
make_win_3seg <- function() {
  data.frame(
    rt_segment_id = rep(1:3, each = 2),
    mz_start  = rep(c(400, 450), 3),
    mz_end    = rep(c(450, 500), 3),
    mz_center = rep(c(425, 475), 3),
    window_width = 50,
    rt_start = rep(c(10, 20, 30), each = 2),
    rt_end   = rep(c(18, 28, 40), each = 2)
  )
}

# --- helper: boundary array ---------------------------------------------------

test_that(".compute_contiguous_rt_schedule tiles segments via midpoints", {
  win <- make_win_3seg()
  sched <- .compute_contiguous_rt_schedule(
    win, acquisition_start_min = 0, acquisition_end_min = 50
  )

  # B = [start, mid12, mid23, end]
  expect_equal(sched$breaks, c(0, 19, 29, 50))
  expect_true(all(diff(sched$breaks) > 0))      # strictly increasing

  # per-window t_start / t_stop, aligned to input row order
  expect_equal(sched$t_start, rep(c(0, 19, 29), each = 2))
  expect_equal(sched$t_stop,  rep(c(19, 29, 50), each = 2))
})
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `Rscript -e "devtools::test(filter='export-contiguous-rt')"`
Expected: FAIL — `could not find function ".compute_contiguous_rt_schedule"`.

- [ ] **Step 3: Implement the helper**

In `R/export_methods.R`, insert this block immediately after the `calculate_loop_n()` function (after its closing `}` near line 44, before the `# Single Strategy CSV Export` banner):

```r
# =============================================================================
# Contiguous RT Schedule (segment-midpoint boundaries)
# =============================================================================

#' Build a gap-free RT schedule from segment midpoints
#'
#' Derives a contiguous boundary array `B` (length k+1 for k RT segments) so the
#' exported method tiles `[acquisition_start_min, acquisition_end_min]` with no
#' gap, overlap, or void. Interior boundaries are the midpoint of adjacent
#' segments' `rt_end`/`rt_start`; the whole array is rounded once (2 decimals) so
#' adjacent segments share an identical boundary at any precision. Reads only
#' `windows$rt_start`/`rt_end` and the segments' sort order — no dependence on
#' `rt_group` contiguity, so it is robust to empty/sparse interior bins.
#'
#' @param windows Data frame with numeric `rt_start`, `rt_end` columns.
#' @param acquisition_start_min Numeric, run start (first segment `t start`).
#' @param acquisition_end_min Numeric or NULL. Run end (last segment `t stop`).
#'   NULL leaves the trailing void open: warns and falls back to the last
#'   segment's `rt_end`.
#'
#' @return List with `breaks` (length k+1), `segments` (distinct sorted
#'   rt_start/rt_end), and `t_start`/`t_stop` vectors aligned to `windows` rows.
#' @keywords internal
#' @noRd
.compute_contiguous_rt_schedule <- function(windows,
                                            acquisition_start_min = 0,
                                            acquisition_end_min = NULL) {
  segs <- windows %>%
    distinct(rt_start, rt_end) %>%
    arrange(rt_start)
  k <- nrow(segs)

  if (is.null(acquisition_end_min)) {
    warning(sprintf(
      "acquisition_end_min not supplied; trailing void left open. Using last segment rt_end (%.2f min) as run end.",
      segs$rt_end[k]), call. = FALSE)
    acquisition_end_min <- segs$rt_end[k]
  }
  if (acquisition_end_min < segs$rt_end[k]) {
    stop(sprintf(
      "acquisition_end_min (%.2f) is before the last segment end (%.2f); cannot end the run before acquired data.",
      acquisition_end_min, segs$rt_end[k]), call. = FALSE)
  }
  if (acquisition_start_min > segs$rt_start[1]) {
    warning(sprintf(
      "acquisition_start_min (%.2f) is after the first segment start (%.2f); leading data will be clipped.",
      acquisition_start_min, segs$rt_start[1]), call. = FALSE)
  }

  B <- numeric(k + 1)
  B[1] <- acquisition_start_min
  B[k + 1] <- acquisition_end_min
  if (k >= 2) {
    for (j in seq_len(k - 1)) {
      B[j + 1] <- (segs$rt_end[j] + segs$rt_start[j + 1]) / 2
    }
  }
  B <- round(B, 2)

  if (!all(diff(B) > 0)) {
    stop(sprintf(
      "Contiguous RT boundaries are not strictly increasing after rounding (segments too close): %s",
      paste(B, collapse = ", ")), call. = FALSE)
  }

  # Map each window row to its segment index, then to [B[j], B[j+1]].
  seg_key <- paste(segs$rt_start, segs$rt_end)
  win_key <- paste(windows$rt_start, windows$rt_end)
  seg_idx <- match(win_key, seg_key)

  list(
    breaks   = B,
    segments = segs,
    t_start  = B[seg_idx],
    t_stop   = B[seg_idx + 1L]
  )
}
```

- [ ] **Step 4: Run the test to verify it passes**

Run: `Rscript -e "devtools::test(filter='export-contiguous-rt')"`
Expected: PASS (1 test, 0 failures).

- [ ] **Step 5: Commit**

```bash
git add R/export_methods.R tests/testthat/test-export-contiguous-rt.R
git commit -m "feat(export): add contiguous RT schedule helper (segment midpoints)"
```

---

## Task 2: Rewrite `export_windows_to_csv()` RT columns + format

Wire the helper into the public export: new `t start`/`t stop` columns, `Adduct="(no adduct)"`, and the two new acquisition arguments.

**Files:**
- Modify: `R/export_methods.R` (`export_windows_to_csv`, signature ~line 66, roxygen ~line 50, mutate/base_cols ~line 100-115)
- Test: `tests/testthat/test-export-contiguous-rt.R` (append)

- [ ] **Step 1: Write the failing tests**

Append to `tests/testthat/test-export-contiguous-rt.R`:

```r
# --- ValidatedData stub for export-level tests --------------------------------

make_vd_stub <- function(n = 200) {
  prec <- data.frame(
    Precursor.Mz = seq(400, 500, length.out = n),
    RT.Apex      = seq(10, 40, length.out = n)
  )
  structure(list(data = prec), class = "ValidatedData")
}

make_ow_3seg <- function() {
  structure(list(windows = make_win_3seg(), parameters = list()),
            class = "OptimizedWindows")
}

# --- export-level format + continuity -----------------------------------------

test_that("export writes t start/t stop columns that tile contiguously", {
  ow <- make_ow_3seg(); vd <- make_vd_stub()
  out <- tempfile(fileext = ".csv")

  export_windows_to_csv(ow, out, vd, acquisition_end_min = 50)
  df <- utils::read.csv(out, check.names = FALSE)

  expect_equal(
    colnames(df),
    c("Compound", "Formula", "Adduct", "m/z", "z",
      "t start (min)", "t stop (min)", "Isolation Window (m/z)")
  )

  ts <- df[["t start (min)"]]; te <- df[["t stop (min)"]]
  expect_equal(unique(ts), c(0, 19, 29))
  expect_equal(unique(te), c(19, 29, 50))
  expect_equal(min(ts), 0)        # first start == acquisition start
  expect_equal(max(te), 50)       # last stop  == acquisition end
  expect_true(all(ts == round(ts, 2)))   # 2-decimal fidelity
  expect_true(all(te == round(te, 2)))
})

test_that("export sets Adduct to '(no adduct)'", {
  ow <- make_ow_3seg(); vd <- make_vd_stub()
  out <- tempfile(fileext = ".csv")
  export_windows_to_csv(ow, out, vd, acquisition_end_min = 50)
  df <- utils::read.csv(out, check.names = FALSE)
  expect_true(all(df$Adduct == "(no adduct)"))
})

test_that("CSV header matches mass_list_example.csv exactly", {
  example_path <- testthat::test_path("..", "..", "mass_list_example.csv")
  skip_if_not(file.exists(example_path), "mass_list_example.csv not present")
  expected_header <- strsplit(readLines(example_path, n = 1), ",")[[1]]

  ow <- make_ow_3seg(); vd <- make_vd_stub()
  out <- tempfile(fileext = ".csv")
  export_windows_to_csv(ow, out, vd, acquisition_end_min = 50)
  actual_header <- strsplit(readLines(out, n = 1), ",")[[1]]

  expect_equal(actual_header, expected_header)
})
```

- [ ] **Step 2: Run the tests to verify they fail**

Run: `Rscript -e "devtools::test(filter='export-contiguous-rt')"`
Expected: FAIL — `export_windows_to_csv()` has no `acquisition_end_min` argument (`unused argument`), and the header still contains `RT Time (min)`/`Window (min)`.

- [ ] **Step 3: Update the signature + roxygen**

In `R/export_methods.R`, add two `@param` lines to the roxygen block for `export_windows_to_csv` (just before `@return NULL (invisible)...`, around line 63):

```r
#' @param acquisition_start_min Numeric, run start in minutes (default: 0). Sets
#'   the first segment's `t start`, closing the leading MS1-only void.
#' @param acquisition_end_min Numeric or NULL (default: NULL). LC method total
#'   length in minutes; sets the last segment's `t stop`, closing the trailing
#'   void. NULL warns and falls back to the last segment's measured `rt_end`.
```

Change the signature (line ~66-68) from:

```r
export_windows_to_csv <- function(optimized_windows, output_file,
                                  validated_data,
                                  charge_state = 1L) {
```

to:

```r
export_windows_to_csv <- function(optimized_windows, output_file,
                                  validated_data,
                                  charge_state = 1L,
                                  acquisition_start_min = 0,
                                  acquisition_end_min = NULL) {
```

- [ ] **Step 4: Replace the RT-column logic in the mutate**

In `export_windows_to_csv`, immediately before the `method_file <- windows_with_counts %>%` line (~line 100), insert the schedule computation:

```r
  # Contiguous RT schedule: gap-free tiling of the acquisition window.
  rt_schedule <- .compute_contiguous_rt_schedule(
    windows_with_counts,
    acquisition_start_min = acquisition_start_min,
    acquisition_end_min   = acquisition_end_min
  )
```

Then replace the `mutate(...)` block (lines ~101-110) so that `Adduct`, the RT columns, and nothing else change:

```r
  method_file <- windows_with_counts %>%
    mutate(
      Compound = .compound,
      Formula = "",
      Adduct = "(no adduct)",
      `m/z` = round(mz_center, 4),
      z = charge_state,
      `t start (min)` = rt_schedule$t_start,
      `t stop (min)`  = rt_schedule$t_stop,
      `Isolation Window (m/z)` = round(mz_end - mz_start, 4)
    )
```

And replace the `base_cols` vector (lines ~112-113):

```r
  base_cols <- c("Compound", "Formula", "Adduct", "m/z", "z",
                 "t start (min)", "t stop (min)", "Isolation Window (m/z)")
```

- [ ] **Step 5: Run the tests to verify they pass**

Run: `Rscript -e "devtools::test(filter='export-contiguous-rt')"`
Expected: PASS (all tests in the file, 0 failures).

- [ ] **Step 6: Confirm existing export tests stay green**

Run: `Rscript -e "devtools::test(filter='export-staggered')"`
Expected: PASS. These tests assert on `Compound`/`z` only (not the renamed RT columns); they call export without `acquisition_end_min`, so each emits the documented NULL-end warning, which `expect_no_error()` tolerates.

- [ ] **Step 7: Regenerate the Rd to match the new signature**

Run: `Rscript -e "devtools::document()"`
Expected: `man/export_windows_to_csv.Rd` updated with the two new `\arguments`. No codoc mismatch.

- [ ] **Step 8: Commit**

```bash
git add R/export_methods.R tests/testthat/test-export-contiguous-rt.R man/export_windows_to_csv.Rd
git commit -m "feat(export): emit contiguous t start/t stop format, Adduct=(no adduct)"
```

---

## Task 3: Edge-case and robustness tests

Lock in the spec's edge cases (spec §5) through the public export path.

**Files:**
- Test: `tests/testthat/test-export-contiguous-rt.R` (append)

- [ ] **Step 1: Write the edge-case tests**

Append to `tests/testthat/test-export-contiguous-rt.R`:

```r
# --- edge cases (spec section 5) ----------------------------------------------

test_that("NULL acquisition_end_min warns and falls back to last rt_end", {
  ow <- make_ow_3seg(); vd <- make_vd_stub()
  out <- tempfile(fileext = ".csv")
  expect_warning(
    export_windows_to_csv(ow, out, vd),   # acquisition_end_min = NULL
    "acquisition_end_min not supplied"
  )
  df <- utils::read.csv(out, check.names = FALSE)
  expect_equal(max(df[["t stop (min)"]]), 40)   # last segment rt_end
})

test_that("acquisition_end_min before last segment end is an error", {
  ow <- make_ow_3seg(); vd <- make_vd_stub()
  out <- tempfile(fileext = ".csv")
  expect_error(
    export_windows_to_csv(ow, out, vd, acquisition_end_min = 35),
    "before the last segment end"
  )
})

test_that("empty interior RT band is absorbed (no coverage hole)", {
  # Only seg1 (10-18) and seg3 (30-40); the 18-30 band has no precursors/segment.
  win <- data.frame(
    rt_segment_id = rep(c(1L, 3L), each = 2),
    mz_start  = rep(c(400, 450), 2),
    mz_end    = rep(c(450, 500), 2),
    mz_center = rep(c(425, 475), 2),
    window_width = 50,
    rt_start = rep(c(10, 30), each = 2),
    rt_end   = rep(c(18, 40), each = 2)
  )
  ow <- structure(list(windows = win, parameters = list()),
                  class = "OptimizedWindows")
  out <- tempfile(fileext = ".csv")
  export_windows_to_csv(ow, out, make_vd_stub(), acquisition_end_min = 50)
  df <- utils::read.csv(out, check.names = FALSE)

  # midpoint of 18 and 30 is 24; schedule must stay contiguous across the gap
  expect_equal(unique(df[["t start (min)"]]), c(0, 24))
  expect_equal(unique(df[["t stop (min)"]]),  c(24, 50))
})

test_that("single segment (k==1) spans the whole run", {
  win <- data.frame(
    rt_segment_id = 1L,
    mz_start = c(400, 450), mz_end = c(450, 500),
    mz_center = c(425, 475), window_width = 50,
    rt_start = 10, rt_end = 40
  )
  ow <- structure(list(windows = win, parameters = list()),
                  class = "OptimizedWindows")
  out <- tempfile(fileext = ".csv")
  export_windows_to_csv(ow, out, make_vd_stub(), acquisition_end_min = 50)
  df <- utils::read.csv(out, check.names = FALSE)
  expect_true(all(df[["t start (min)"]] == 0))
  expect_true(all(df[["t stop (min)"]]  == 50))
})

test_that("staggered cycles collapse to one RT segment", {
  win <- generate_staggered_windows_internal(
    mz_min = 400, mz_max = 500, n_windows = 5,
    min_width_da = 4, max_width_da = 80, rt_bin_index = 1, fz_offset = 0.25
  )
  win$rt_segment_id <- 1L
  win$rt_start <- 10
  win$rt_end   <- 40
  ow <- structure(list(windows = win, parameters = list(fz_offset = 0.25)),
                  class = "OptimizedWindows")
  out <- tempfile(fileext = ".csv")
  export_windows_to_csv(ow, out, make_vd_stub(), acquisition_end_min = 50)
  df <- utils::read.csv(out, check.names = FALSE)
  # both cycles share (rt_start, rt_end) -> distinct() yields one segment
  expect_equal(length(unique(df[["t start (min)"]])), 1L)
  expect_equal(length(unique(df[["t stop (min)"]])),  1L)
})
```

- [ ] **Step 2: Run the tests to verify they pass**

Run: `Rscript -e "devtools::test(filter='export-contiguous-rt')"`
Expected: PASS (all tests, 0 failures). These exercise behavior already implemented in Tasks 1–2; if any fail, fix the helper/export rather than the test.

- [ ] **Step 3: Commit**

```bash
git add tests/testthat/test-export-contiguous-rt.R
git commit -m "test(export): cover NULL/short end, empty bin, k==1, staggered collapse"
```

---

## Task 4: Thread acquisition bounds through `main.R`

**Files:**
- Modify: `main.R` (`run_complete_pipeline` signature ~line 37-54, roxygen ~line 28, export call ~line 246-250)

- [ ] **Step 1: Add the parameters to the signature**

In `main.R`, add two arguments to `run_complete_pipeline()`. Insert after `rt_binning_mode = "fixed",` (line 47):

```r
  acquisition_start_min = 0,
  acquisition_end_min = NULL,
```

- [ ] **Step 2: Add roxygen for the new params**

In `main.R`, add after the `@param rt_bin_width_min ...` line (~line 27):

```r
#' @param acquisition_start_min Run start in minutes (default: 0). First segment's t start in the exported method.
#' @param acquisition_end_min LC method total length in minutes (default: NULL). Last segment's t stop; NULL warns and uses the last measured rt_end.
```

- [ ] **Step 3: Pass them to the export call**

In `main.R`, change the `export_windows_to_csv(...)` call (lines ~246-250) from:

```r
      export_windows_to_csv(
        optimized_windows = windows_result,
        output_file = method_path,
        validated_data = validated_data
      )
```

to:

```r
      export_windows_to_csv(
        optimized_windows = windows_result,
        output_file = method_path,
        validated_data = validated_data,
        acquisition_start_min = acquisition_start_min,
        acquisition_end_min = acquisition_end_min
      )
```

- [ ] **Step 4: Verify main.R parses**

Run: `Rscript -e "parse('main.R'); cat('main.R parses OK\n')"`
Expected: `main.R parses OK` with no error.

- [ ] **Step 5: Commit**

```bash
git add main.R
git commit -m "feat(pipeline): thread acquisition_start/end into run_complete_pipeline export"
```

---

## Task 5: Thread acquisition end through the Shiny app

Expose the run length in Step 3 and pass it to both `export_windows_to_csv()` call sites, and refresh the format preview text.

**Files:**
- Modify: `inst/shiny_app/ui_step3_results.R` (download box ~line 142-167)
- Modify: `inst/shiny_app/server_downloads.R` (preview ~line 88-92; `download_method` thermo branch ~line 124-128; batch zip thermo export ~line 230-234)

- [ ] **Step 1: Add the run-length input to the UI**

In `inst/shiny_app/ui_step3_results.R`, the download box's `fluidRow` currently has four `column(3, ...)` cells (sample name, condition, export format, download button). Change the download-button cell from `column(3, ...)` to `column(2, ...)` and insert a new `column(2, ...)` for the run length before it. Replace lines ~151-166 (the `selectInput("export_format"...)` cell through the download-button cell) with:

```r
            column(3,
              selectInput("export_format", "Export Format",
                choices = c(
                  "Thermo Targeted Mass List" = "thermo",
                  "Center Mass List" = "center_mass",
                  "m/z Range List" = "mz_range"
                ),
                selected = "thermo"
              )
            ),
            column(2,
              numericInput("acquisition_end_min", "Run Length (min)",
                           value = NA, min = 0, step = 1)
            ),
            column(2,
              div(style = "padding-top: 25px;",
                downloadButton("download_method", "Download Method",
                               class = "btn-success btn-block")
              )
            )
```

(The two `column(3, ...)` cells for sample name and condition above this stay unchanged; column widths now sum to 3+3+3+2+2 = 13, which bs4Dash wraps gracefully — acceptable for this control row. If strict 12-col alignment is preferred, change the two leading cells to `column(2, ...)`.)

- [ ] **Step 2: Pass the run length into the `download_method` thermo branch**

In `inst/shiny_app/server_downloads.R`, change the thermo branch of `download_method` (lines ~121-128) from:

```r
      if (fmt == "thermo") {
        req(rv$validated_data)

        export_windows_to_csv(
          optimized_windows = rv$optimized_windows,
          output_file = file,
          validated_data = rv$validated_data
        )
```

to:

```r
      if (fmt == "thermo") {
        req(rv$validated_data)

        acq_end <- input$acquisition_end_min
        if (is.na(acq_end %||% NA)) acq_end <- NULL

        export_windows_to_csv(
          optimized_windows = rv$optimized_windows,
          output_file = file,
          validated_data = rv$validated_data,
          acquisition_end_min = acq_end
        )
```

- [ ] **Step 3: Pass the run length into the batch-zip thermo export**

In `inst/shiny_app/server_downloads.R`, change the batch-zip thermo export (lines ~230-234) from:

```r
        export_windows_to_csv(
          optimized_windows = rv$optimized_windows,
          output_file = file.path(batch_dir, "thermo.csv"),
          validated_data = rv$validated_data
        )
```

to:

```r
        acq_end <- input$acquisition_end_min
        if (is.na(acq_end %||% NA)) acq_end <- NULL

        export_windows_to_csv(
          optimized_windows = rv$optimized_windows,
          output_file = file.path(batch_dir, "thermo.csv"),
          validated_data = rv$validated_data,
          acquisition_end_min = acq_end
        )
```

- [ ] **Step 4: Refresh the Thermo preview text**

In `inst/shiny_app/server_downloads.R`, update the `thermo` case of `output$export_format_preview` (lines ~88-92) so the sample matches the new format. Replace:

```r
      thermo = format_preview_card(
        "Thermo Targeted Mass List", "Xcalibur-compatible CSV",
        "Compound,Formula,Adduct,m/z,z,RT Time (min),Window (min),Isolation Window (m/z)\n1,,,425.2523,0,4.0,5.0,50.5046",
        "8-column targeted mass list format"
      ),
```

with:

```r
      thermo = format_preview_card(
        "Thermo Targeted Mass List", "Xcalibur-compatible CSV",
        "Compound,Formula,Adduct,m/z,z,t start (min),t stop (min),Isolation Window (m/z)\n1,,(no adduct),425.2523,1,0,5,50.5046",
        "8-column targeted mass list, contiguous t start/t stop schedule"
      ),
```

- [ ] **Step 5: Verify both Shiny files parse**

Run: `Rscript -e "parse('inst/shiny_app/ui_step3_results.R'); parse('inst/shiny_app/server_downloads.R'); cat('shiny modules parse OK\n')"`
Expected: `shiny modules parse OK` with no error.

- [ ] **Step 6: Commit**

```bash
git add inst/shiny_app/ui_step3_results.R inst/shiny_app/server_downloads.R
git commit -m "feat(shiny): add Run Length input, thread acquisition end into Thermo export"
```

---

## Task 6: Update format documentation

**Files:**
- Modify: `CLAUDE.md` (line ~272)
- Modify: `docs/domain-knowledge.md` (lines ~476-483)

- [ ] **Step 1: Update CLAUDE.md export description**

In `CLAUDE.md`, replace the line (~272):

```
`export_windows_to_csv()` in `R/export_methods.R` produces an Xcalibur-compatible Targeted Mass List CSV with 8 columns: Compound, Formula, Adduct, m/z, z, RT Time (min), Window (min), Isolation Window (m/z). Simplified from the earlier wide format to the columns that import cleanly into Xcalibur.
```

with:

```
`export_windows_to_csv()` in `R/export_methods.R` produces an Xcalibur-compatible Targeted Mass List CSV with 8 columns: Compound, Formula, Adduct, m/z, z, t start (min), t stop (min), Isolation Window (m/z). The RT schedule is a **contiguous tiling** of `[acquisition_start_min, acquisition_end_min]`: segment boundaries are the midpoint of adjacent segments' measured RT spans, rounded once, so there are zero gaps/overlaps/voids (no MS1-only dead zones). `Adduct` is `(no adduct)`. Pass `acquisition_end_min` (LC method length) to close the trailing void.
```

- [ ] **Step 2: Update domain-knowledge.md Format A block**

In `docs/domain-knowledge.md`, replace lines ~476-483 (the **Format A** block):

```
**Format A: Thermo Targeted Mass List** (existing — `export_windows_to_csv()`)
```
Compound, Formula, Adduct, m/z, z, RT Time (min), Window (min),
Isolation Window (m/z)
```
- 8 columns, Xcalibur import compatible
- Simplified (v0.4.x) to the columns that import cleanly into Xcalibur;
  earlier AIDIA metadata + acquisition-parameter columns were removed
```

with:

```
**Format A: Thermo Targeted Mass List** (existing — `export_windows_to_csv()`)
```
Compound, Formula, Adduct, m/z, z, t start (min), t stop (min),
Isolation Window (m/z)
```
- 8 columns, Xcalibur import compatible (`Adduct = "(no adduct)"`)
- RT schedule is a **contiguous tiling** of `[acquisition_start_min,
  acquisition_end_min]`: interior boundaries are adjacent-segment midpoints,
  rounded once → zero gap/overlap/void (no MS1-only dead zones). Pass
  `acquisition_end_min` to close the trailing void; leading void closes at 0.
```

- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md docs/domain-knowledge.md
git commit -m "docs: describe contiguous t start/t stop export format"
```

---

## Task 7: Full regression

**Files:** none (verification only)

- [ ] **Step 1: Run the full unit-test suite**

Run: `Rscript -e "devtools::test()"`
Expected: All tests pass (0 failures, 0 errors). In particular `test-export-contiguous-rt` and `test-export-staggered` are green.

- [ ] **Step 2: Run the end-to-end pipeline test (requires real data in `data/`)**

Run: `Rscript -e "source('tests/manual/test_full_pipeline.R')"`
Expected: Pipeline completes; exported method CSVs now carry `t start (min)`/`t stop (min)` headers. If `data/` has no `*min_report.parquet`, this step is N/A — note that in the final report instead of marking it passed.

- [ ] **Step 3: Spot-check a generated method file**

Open one exported CSV under the pipeline `output_*` directory (or generate one from the Shiny app) and confirm: header is the 8-column `t start`/`t stop` form; first `t start` equals the acquisition start; adjacent segments' `t stop`/`t start` are identical (no gap/overlap); `Adduct` is `(no adduct)`.

- [ ] **Step 4: Final commit (only if Step 1–3 surfaced fixes)**

```bash
git add -A
git commit -m "fix(export): address regression findings from full-pipeline run"
```

(Skip if nothing changed.)

---

## Self-review (completed against the spec)

**Spec coverage:**
- §3.1 new params (`acquisition_start_min`, `acquisition_end_min`) → Task 2 (signature), Task 4 (main.R), Task 5 (Shiny). ✓
- §3.2 data source = existing `windows$rt_start/rt_end`, no S3 change → helper reads only those columns (Task 1); no edits to `R/s3_classes.R`/`R/window_optimization.R`. ✓
- §3.3 midpoint algorithm + round-once + monotonicity `stopifnot` → Task 1 helper. ✓
- §3.4 exact CSV format (header, `Adduct="(no adduct)"`, `t start`/`t stop`, `Isolation Window` = `round(mz_end-mz_start,4)`) → Task 2; header pinned to example file by test. ✓ (`m/z`, `z`, `Compound` unchanged — preserved.)
- §5 edge cases (NULL end warn, end<last error, empty interior bin, k==1, staggered collapse) → Task 3. The `acquisition_start_min > segs$rt_start[1]` case warns (helper Step 3); spec allows "error/warning" and notes it normally can't fire (start defaults 0). ✓
- §6 affected files (export_methods.R, main.R, Shiny ui/server, tests, CLAUDE.md, domain-knowledge.md) → Tasks 2/4/5/6; deliberately NOT touching `window_optimization.R`/`s3_classes.R`. ✓
- §7 test plan (continuity, format, empty-bin, edges, regression) → Tasks 2/3/7. ✓

**Placeholder scan:** No TBD/TODO/"handle edge cases" — every code step contains complete code. ✓

**Type/name consistency:** Helper named `.compute_contiguous_rt_schedule` everywhere; returns `breaks`/`segments`/`t_start`/`t_stop`; columns `t start (min)`/`t stop (min)` identical in helper-free export, tests, docs, and preview. Arg names `acquisition_start_min`/`acquisition_end_min` consistent across export, main.R, Shiny. ✓

**Out of scope (per spec §2, §9):** m/z edge-padding (deferred), existing-acquisition trim (done separately), other export formats (`export_center_mass_list`, `export_mz_range_list`) — untouched. ✓
