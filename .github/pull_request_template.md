## Change

Describe the problem and the resulting behavior.

## Validation

- [ ] R CMD check passes for the built and installed package (not only `devtools::test()`).
- [ ] Any test-only dependencies are declared in `Suggests`.
- [ ] Packaged resources use `system.file(..., mustWork = TRUE)` in tests.
- [ ] All five required CI checks pass against the latest main before merging.

Include relevant check results and explain any remaining NOTEs.
