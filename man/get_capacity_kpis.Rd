% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capacity_kpis.R
\name{get_capacity_kpis}
\alias{get_capacity_kpis}
\title{Derive Acquisition Capacity KPIs}
\usage{
get_capacity_kpis(
  plan,
  windows,
  evaluation = NULL,
  thresholds = capacity_kpi_thresholds()
)
}
\arguments{
\item{plan}{OptimizationPlan S3 object (Stage 2 output).}

\item{windows}{OptimizedWindows S3 object (Stage 3 output). Currently
reserved for forward compatibility; all required information is read
from \code{plan} and \code{evaluation}.}

\item{evaluation}{Optional list returned by \code{\link[=evaluate_windows]{evaluate_windows()}}. When
\code{NULL}, \code{filled_window_ratio} is \code{NA_real_}.}

\item{thresholds}{Threshold list from \code{\link[=capacity_kpi_thresholds]{capacity_kpi_thresholds()}}
(default).}
}
\value{
Named list with elements:
\describe{
\item{values}{Named numeric vector with \code{filled_window_ratio},
\code{dppp_headroom_x}, \code{cycle_time_headroom_pct},
\code{window_count_headroom_pct}.}
\item{grades}{Named character vector with the same names, each set
to \code{"Bad"}, \code{"Warn"}, \code{"OK"}, \code{"Info"}, or \code{"NA"}.}
\item{is_spec_limited}{Logical scalar. When \code{TRUE}, the
\code{window_count_headroom_pct} value has been clamped to \code{0} and the
gauge label should append \code{"(spec-limited)"}.}
\item{thresholds}{The threshold list used (echoed for downstream
consumers).}
}
}
\description{
Computes the four capacity KPIs (filled window ratio, DPPP headroom,
cycle-time headroom, window-count headroom) from existing AIDIA outputs.
Pure derivation — no S3 mutation, no caching. Mirrors the
\code{\link[=evaluate_windows]{evaluate_windows()}} pattern: the Shiny app calls this as a reactive,
scripts call it directly.
}
\section{Field sources}{

\itemize{
\item Filled window ratio: \code{1 - length(evaluation$quality_flags$empty_windows) / evaluation$overall$n_total_windows}.
\item DPPP headroom: \code{plan$diagnosis$current_dppp_median / plan$parameters$target_dppp}.
\item Cycle-time headroom: \code{(plan$required_cycle_time_sec - plan$actual_cycle_time_sec) / plan$required_cycle_time_sec * 100}.
\item Window-count headroom: \code{(plan$parameters$max_windows - plan$window_count_per_bin) / plan$parameters$max_windows * 100}.
When \code{plan$it_optimization$is_spec_limited} is \code{TRUE}, the value is
forced to \code{0} and \code{is_spec_limited} is propagated for label decoration.
}
}

\section{Edge cases}{

When \code{evaluation} is \code{NULL} or its quality flag / overall fields are
missing, \code{filled_window_ratio} is \code{NA_real_} and downstream bottleneck
rules #2 / #3 are skipped. The remaining three KPIs are unaffected.
}

\examples{
\dontrun{
kpis <- get_capacity_kpis(plan, windows, evaluation = evaluate_windows(
  optimized_windows = windows,
  validated_data    = validated,
  optimization_plan = plan
))
kpis$values
kpis$grades
}
}
