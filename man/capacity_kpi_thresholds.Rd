% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capacity_kpis.R
\name{capacity_kpi_thresholds}
\alias{capacity_kpi_thresholds}
\title{Default Acquisition Capacity KPI Thresholds}
\usage{
capacity_kpi_thresholds(
  filled_window_ratio = NULL,
  dppp_headroom_x = NULL,
  cycle_time_headroom_pct = NULL,
  window_count_headroom_pct = NULL
)
}
\arguments{
\item{filled_window_ratio}{List with \code{bad}, \code{warn} numeric cutoffs
(proportions in 0..1).}

\item{dppp_headroom_x}{List with \code{bad}, \code{info} numeric cutoffs (fold).}

\item{cycle_time_headroom_pct}{List with \code{bad}, \code{info} numeric cutoffs (\%).}

\item{window_count_headroom_pct}{List with \code{info} numeric cutoff (\%).}
}
\value{
Named list with one entry per KPI. Each entry is itself a named
list of numeric cutoffs. Suitable for passing to \code{\link[=get_capacity_kpis]{get_capacity_kpis()}}
via the \code{thresholds} argument.
}
\description{
Constructs the named nested list of thresholds consumed by
\code{\link[=get_capacity_kpis]{get_capacity_kpis()}} and \code{\link[=classify_capacity_kpi]{classify_capacity_kpi()}}. Override individual
KPIs by passing a list with the same name; missing keys fall back to the
published defaults.
}
\section{Default table}{

\tabular{lllll}{
KPI                          \tab Bad   \tab Warn  \tab OK         \tab Info \cr
\code{filled_window_ratio}        \tab <0.70 \tab 0.70-0.90 \tab >=0.90  \tab --   \cr
\code{dppp_headroom_x}            \tab <1    \tab --    \tab 1-2        \tab >=2  \cr
\code{cycle_time_headroom_pct}    \tab <0    \tab --    \tab 0-15       \tab >=15 \cr
\code{window_count_headroom_pct}  \tab --    \tab --    \tab 0-30       \tab >=30 \cr
}
}

\examples{
capacity_kpi_thresholds()
capacity_kpi_thresholds(
  filled_window_ratio = list(bad = 0.60, warn = 0.85),
  dppp_headroom_x     = list(bad = 1.0,  info = 3.0)
)
}
