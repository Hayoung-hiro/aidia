% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_capacity_kpis.R
\name{plot_capacity_kpis}
\alias{plot_capacity_kpis}
\title{Plot Acquisition Capacity KPI Gauges}
\usage{
plot_capacity_kpis(
  kpis,
  thresholds = kpis$thresholds \%||\% capacity_kpi_thresholds()
)
}
\arguments{
\item{kpis}{List returned by \code{\link[=get_capacity_kpis]{get_capacity_kpis()}}.}

\item{thresholds}{Threshold list from \code{\link[=capacity_kpi_thresholds]{capacity_kpi_thresholds()}}
(defaults to \code{kpis$thresholds}).}
}
\value{
A \code{ggplot} object.
}
\description{
Renders the four capacity KPIs from \code{\link[=get_capacity_kpis]{get_capacity_kpis()}} as a single
ggplot strip of four half-circle gauges drawn from direct cartesian
polygons (no \code{coord_polar}). Each gauge has an outer-rim
\code{Bad / Warn / OK / Info} zone band (light tints, scale reference) and an
inner ring filled to the current value in the grade color. Center text
shows the raw value. The DPPP gauge visually saturates at 5x; the
center text shows the actual value (Astral commonly reports >= 30x).
When \code{kpis$is_spec_limited} is TRUE, the window-headroom panel title is
annotated \code{(spec-limited)}.
}
\details{
Shiny-only by design (no PDF report entry). Called from
\code{output$capacity_dashboard} in \code{inst/shiny_app/server_optimization.R}.
}
\examples{
\dontrun{
kpis <- get_capacity_kpis(plan, windows, evaluation)
plot_capacity_kpis(kpis)
}
}
