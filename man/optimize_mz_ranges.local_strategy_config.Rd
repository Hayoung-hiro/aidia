% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mz_optimization.R
\name{optimize_mz_ranges.local_strategy_config}
\alias{optimize_mz_ranges.local_strategy_config}
\title{Optimize m/z Ranges via Per-RT-Bin Iteration (LOCAL Strategies)}
\usage{
\method{optimize_mz_ranges}{local_strategy_config}(
  config,
  precursor_data,
  rt_stats,
  n_windows_per_bin = 10,
  min_width_da = 2,
  mz_range_min = 400,
  mz_range_max = 1200,
  ...
)
}
\arguments{
\item{config}{A strategy_config object (use \code{\link{greedy_config}},
\code{\link{kde_config}}, \code{\link{quantile_config}},
\code{\link{coverage_config}}, or \code{\link{outlier_config}}).}

\item{precursor_data}{Data frame with at least \code{rt_group} and
\code{Precursor.Mz} columns.}

\item{rt_stats}{Data frame of RT bin statistics with \code{rt_start},
\code{rt_end} columns (one row per bin).}

\item{n_windows_per_bin}{Integer, number of MS2 windows per RT bin.
Required by greedy strategy (determines fixed m/z range per cycle).}

\item{min_width_da}{Numeric, minimum isolation window width in Da.
Used by greedy strategy.}

\item{mz_range_min}{Numeric, fallback minimum m/z for empty RT bins.}

\item{mz_range_max}{Numeric, fallback maximum m/z for empty RT bins.}

\item{...}{Additional arguments passed to method.}
}
\value{
Data frame with m/z ranges per RT segment.
}
\description{
Parent S3 method for all \code{local_strategy_config} subclasses
(quantile, coverage, outlier). Iterates over RT bins, dispatches the
per-bin computation via \code{\link{compute_mz_range_for_bin}}, then
assembles the result data frame.
}
\details{
Subclasses do NOT override this method; they only implement
\code{compute_mz_range_for_bin.*_config()}.
}
\keyword{internal}
