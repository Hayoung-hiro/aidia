% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage3_window_optimization.R
\name{optimize_windows}
\alias{optimize_windows}
\title{Optimize DIA Isolation Windows (Stage 3 Main Function)}
\usage{
optimize_windows(
  validated_data,
  optimization_plan,
  rt_bin_width_min = 5,
  mz_strategy = "quantile",
  window_mode = "variable",
  target_coverage = 0.95,
  quantile_lower = 0.05,
  quantile_upper = 0.95,
  outlier_threshold = 3,
  smoothing_window = 7,
  polynomial_order = 3,
  min_width_da = 2,
  max_width_da = 80,
  overlap_percentage = 0
)
}
\arguments{
\item{validated_data}{ValidatedData object from Stage 1}

\item{optimization_plan}{OptimizationPlan object from Stage 2}

\item{rt_bin_width_min}{Numeric, RT bin width in minutes (default: 5)
\itemize{
\item Recommended: 3-7 minutes for typical proteomics gradients
\item Shorter bins: Better RT specificity, more bins to manage
\item Longer bins: Simpler, fewer total windows
}}

\item{mz_strategy}{Character, m/z optimization strategy (default: "quantile")
\itemize{
\item "quantile": Use percentiles (P5-P95), fast and robust
\item "coverage": Minimum range for target coverage, conservative
\item "outlier": Mean +/- 3*SD, removes outliers
\item "smoothing": GLOBAL Savitzky-Golay smoothing across RT
}}

\item{window_mode}{Character, window generation mode (default: "variable")
\itemize{
\item "fixed": Equal-width windows
\item "variable": Density-based adaptive windows (recommended)
}}

\item{target_coverage}{Numeric, target m/z coverage 0-1 (default: 0.95)}

\item{quantile_lower}{Numeric, lower quantile for quantile strategy (default: 0.05)}

\item{quantile_upper}{Numeric, upper quantile for quantile strategy (default: 0.95)}

\item{outlier_threshold}{Numeric, SD multiplier for outlier strategy (default: 3.0)}

\item{smoothing_window}{Integer, SG window size for smoothing (default: 7)}

\item{polynomial_order}{Integer, SG polynomial order (default: 3)}

\item{min_width_da}{Numeric, minimum window width in Da (default: 2)}

\item{max_width_da}{Numeric, maximum window width in Da (default: 80)}

\item{overlap_percentage}{Numeric, overlap \% between windows (default: 0)}
}
\value{
OptimizedWindows S3 object
}
\description{
Performs complete window optimization including RT binning, m/z range
optimization, and window generation in a single integrated workflow.
}
\details{
This function operates per-RT-bin: each RT bin independently generates
the specified number of windows, resulting in total_windows = n_bins x n_windows.
}
\examples{
# Basic usage
windows <- optimize_windows(
  validated_data,
  optimization_plan,
  rt_bin_width_min = 5,
  mz_strategy = "quantile",
  window_mode = "variable"
)

# Conservative settings
windows <- optimize_windows(
  validated_data,
  optimization_plan,
  rt_bin_width_min = 3,
  mz_strategy = "coverage",
  target_coverage = 0.98,
  window_mode = "variable"
)
}
