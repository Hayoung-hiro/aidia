% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/window_optimization.R
\name{optimize_windows}
\alias{optimize_windows}
\title{Optimize DIA Isolation Windows (Stage 3 Main Function)}
\usage{
optimize_windows(
  validated_data,
  optimization_plan,
  strategy_config = NULL,
  rt_bin_width_min = 5,
  mz_strategy = "quantile",
  window_mode = "density",
  mz_range_min = 400,
  mz_range_max = 1200,
  target_coverage = 0.95,
  quantile_lower = 0.05,
  quantile_upper = 0.95,
  outlier_threshold = 3,
  smoothing_window = 7,
  polynomial_order = 3,
  min_width_da = 2,
  max_width_da = 80,
  overlap_percentage = 0,
  mz_step = 0.5,
  n_windows_override = NULL,
  greedy_apply_smoothing = TRUE,
  kde_density_threshold = 0.1,
  kde_min_coverage = 0.8,
  quantile_apply_smoothing = FALSE,
  outlier_apply_smoothing = FALSE,
  fz_offset = 0.25,
  coverage_mode = "narrowest",
  rt_binning_mode = "fixed",
  cpd_min_bin_width = 1,
  cpd_max_bin_width = 15,
  cpd_min_precursors_per_bin = 50,
  cpd_significance_level = 0.05,
  edge_void_buffer_min = 0.5,
  edge_wash_min_precursors = 30,
  width_grid_step = 0.5,
  smoothing_method = "whittaker",
  whittaker_lambda = 10
)
}
\arguments{
\item{validated_data}{ValidatedData object from Stage 1}

\item{optimization_plan}{OptimizationPlan object from Stage 2}

\item{strategy_config}{A strategy_config object created by one of:
\code{\link[=greedy_config]{greedy_config()}}, \code{\link[=quantile_config]{quantile_config()}}, \code{\link[=coverage_config]{coverage_config()}},
\code{\link[=outlier_config]{outlier_config()}}, \code{\link[=kde_config]{kde_config()}}. When provided, overrides
mz_strategy and all strategy-specific parameters below.}

\item{rt_bin_width_min}{Numeric, RT bin width in minutes (default: 5)
\itemize{
\item Recommended: 3-7 minutes for typical proteomics gradients
\item Shorter bins: Better RT specificity, more bins to manage
\item Longer bins: Simpler, fewer total windows
}}

\item{mz_strategy}{Character, m/z optimization strategy (default: "quantile").
Ignored when strategy_config is provided.
\itemize{
\item "greedy": MacCoss Lab algorithm, maximize precursor count (recommended)
\item "kde": Kernel Density Estimation, find density peak and expand
\item "quantile": Use percentiles (P5-P95), fast and robust
\item "coverage": Minimum range for target coverage, conservative
\item "outlier": Mean +/- 3*SD, removes outliers
}}

\item{window_mode}{Character, window generation mode (default: "density")
\itemize{
\item "fixed": Equal-width windows
\item "density": Density-based adaptive windows (recommended)
}}

\item{mz_range_min}{Numeric, fallback minimum m/z for empty RT bins (default: 400)}

\item{mz_range_max}{Numeric, fallback maximum m/z for empty RT bins (default: 1200)}

\item{target_coverage}{Numeric, target m/z coverage 0-1 (default: 0.95)}

\item{quantile_lower}{Numeric, lower quantile for quantile strategy (default: 0.05)}

\item{quantile_upper}{Numeric, upper quantile for quantile strategy (default: 0.95)}

\item{outlier_threshold}{Numeric, SD multiplier for outlier strategy (default: 3.0)}

\item{smoothing_window}{Integer, SG window size for smoothing (default: 7)}

\item{polynomial_order}{Integer, SG polynomial order (default: 3)}

\item{min_width_da}{Numeric, minimum window width in Da (default: 2)}

\item{max_width_da}{Numeric, maximum window width in Da (default: 80)}

\item{overlap_percentage}{Numeric, overlap \% between windows (default: 0)}

\item{mz_step}{Numeric, step size for greedy sliding window in Da (default: 0.5)}

\item{n_windows_override}{Integer or NULL, override window count (for Greedy strategy)}

\item{greedy_apply_smoothing}{Logical, apply SG smoothing to greedy boundaries (default: TRUE)}

\item{kde_density_threshold}{Numeric, KDE density threshold 0-1 (default: 0.1)}

\item{kde_min_coverage}{Numeric, KDE minimum coverage 0-1 (default: 0.80)}

\item{quantile_apply_smoothing}{Logical, apply SG smoothing to quantile boundaries (default: FALSE)}

\item{outlier_apply_smoothing}{Logical, apply SG smoothing to outlier boundaries (default: FALSE)}

\item{fz_offset}{Numeric, forbidden zone offset for staggered mode (default: 0.25, use 0.18 for phospho)}

\item{coverage_mode}{Character, coverage strategy mode: "narrowest" or "centered"}

\item{rt_binning_mode}{Character, RT binning mode: "fixed" or "adaptive" (default: "fixed")}

\item{cpd_min_bin_width}{Numeric, minimum RT bin width in minutes for adaptive binning (default: 1.0)}

\item{cpd_max_bin_width}{Numeric, maximum RT bin width in minutes for adaptive binning (default: 15.0)}

\item{cpd_min_precursors_per_bin}{Integer, minimum precursors per bin for adaptive binning (default: 50)}

\item{cpd_significance_level}{Numeric, significance level for changepoint detection (default: 0.05)}

\item{edge_void_buffer_min}{Numeric, buffer in minutes for edge bins with no precursors (default: 0.5)}

\item{edge_wash_min_precursors}{Integer, minimum precursors for edge bin retention (default: 30)}

\item{width_grid_step}{Numeric, grid step for width digitization in Da (default: 0.5)
\itemize{
\item Snaps window widths to nearest multiple for batch reproducibility
\item Set to NULL or 0 to disable digitization
\item Only applies to density (variable) window mode
}}
}
\value{
OptimizedWindows S3 object
}
\description{
Performs complete window optimization including RT binning, m/z range
optimization, and window generation in a single integrated workflow.
}
\details{
This function operates per-RT-bin: each RT bin independently generates
the specified number of windows, resulting in total_windows = n_bins x n_windows.
}
\examples{
\dontrun{
# New way: strategy config objects (recommended)
windows <- optimize_windows(
  validated_data, optimization_plan,
  strategy_config = greedy_config(apply_smoothing = TRUE)
)

# Legacy way: flat parameters (still works)
windows <- optimize_windows(
  validated_data, optimization_plan,
  mz_strategy = "quantile", window_mode = "density"
)
}
}
