% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage3_mz_optimization.R
\name{optimize_mz_ranges_greedy_internal}
\alias{optimize_mz_ranges_greedy_internal}
\title{Optimize m/z Ranges with GREEDY Strategy (Internal)}
\usage{
optimize_mz_ranges_greedy_internal(
  precursor_data,
  rt_stats,
  n_windows_per_bin,
  min_width_da,
  mz_step = 0.5,
  apply_smoothing = TRUE,
  smoothing_window = 5,
  polynomial_order = 2,
  target_coverage = 0.9,
  mz_range_min = 400,
  mz_range_max = 1200
)
}
\arguments{
\item{precursor_data}{Data frame with rt_group and Precursor.Mz columns}

\item{rt_stats}{RT statistics data frame}

\item{n_windows_per_bin}{Integer, number of windows per RT bin}

\item{min_width_da}{Numeric, minimum window width in Da (determines fixed range)}

\item{mz_step}{Numeric, step size for sliding window in Da (default: 0.5)}

\item{apply_smoothing}{Logical, whether to apply SG smoothing (default: TRUE)}

\item{smoothing_window}{Integer, SG window size (default: 5)}

\item{polynomial_order}{Integer, SG polynomial order (default: 2)}

\item{target_coverage}{Not used (kept for API consistency)}
}
\value{
Data frame with m/z ranges per RT segment
}
\description{
GREEDY optimization based on MacCoss Lab dynamicDIA.py algorithm.
}
\details{
\strong{Key Concept}: The m/z range per cycle is FIXED by instrument constraints:
mz_range_per_cycle = n_windows × isolation_width

The algorithm slides this fixed range across the m/z axis to find the
position that covers the MAXIMUM number of precursors.

\strong{Post-processing}: Following the original dynamicDIA implementation,
Savitzky-Golay smoothing is applied to m/z boundaries across RT bins
to prevent abrupt jumps between adjacent bins.

\strong{Difference from Coverage strategy}:
\itemize{
\item Coverage: Fixed target coverage → Variable m/z range
\item Greedy:   Fixed m/z range → Variable coverage (maximized within constraint)
}

Reference: https://github.com/uw-maccosslab/manuscript-dynamic-dia
}
\keyword{internal}
