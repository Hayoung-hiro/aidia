% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_boundary.R
\name{bootstrap_boundary_ci}
\alias{bootstrap_boundary_ci}
\title{Bootstrap Confidence Intervals for m/z Boundaries}
\usage{
bootstrap_boundary_ci(
  validated_data,
  optimization_plan,
  strategy_config = greedy_config(),
  n_boot = 200,
  ci_level = 0.95,
  rt_bin_width_min = 5,
  rt_bin_width_min_override = NULL,
  seed = 42,
  verbose = TRUE
)
}
\arguments{
\item{validated_data}{ValidatedData object from Stage 1}

\item{optimization_plan}{OptimizationPlan object from Stage 2}

\item{strategy_config}{A strategy_config object (e.g., greedy_config())}

\item{n_boot}{Integer, number of bootstrap iterations (default: 200)}

\item{ci_level}{Numeric, confidence level 0-1 (default: 0.95 for 95\% CI)}

\item{rt_bin_width_min}{Numeric, RT bin width in minutes (default: 5)}

\item{rt_bin_width_min_override}{Numeric or NULL, override rt_bin_width_min (default: NULL)}

\item{seed}{Integer or NULL, random seed for reproducibility (default: 42)}

\item{verbose}{Logical, print progress (default: TRUE)}
}
\value{
A list with class "boundary_ci" containing:
\item{ci_data}{Data frame with per-RT-bin CI bounds}
\item{boot_matrix_min}{Matrix of bootstrap mz_min values (n_boot x n_bins)}
\item{boot_matrix_max}{Matrix of bootstrap mz_max values (n_boot x n_bins)}
\item{observed}{Data frame of observed (non-bootstrap) boundaries}
\item{params}{List of parameters used}
}
\description{
Estimates boundary uncertainty by resampling precursors within each RT bin
and recalculating m/z boundaries. Returns percentile-based confidence
intervals for both mz_min and mz_max across RT segments.
}
\examples{
\dontrun{
ci <- bootstrap_boundary_ci(
  validated_data, plan,
  strategy_config = greedy_config(),
  n_boot = 200
)
plot_boundary_ci(ci, validated_data)
}
}
