% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instrument_utils.R
\name{resolve_injection_time}
\alias{resolve_injection_time}
\title{Resolve Injection Time (Handle 'auto' Mode)}
\usage{
resolve_injection_time(ms2_time, resolution = NULL, analyzer_type = "orbitrap")
}
\arguments{
\item{ms2_time}{Injection time in ms, or "auto" string}

\item{resolution}{Numeric, Orbitrap resolution (required when ms2_time is "auto")}

\item{analyzer_type}{Character, analyzer type (default: "orbitrap")}
}
\value{
Numeric, resolved injection time in milliseconds
}
\description{
Resolves the injection time value, handling the special 'auto' mode
used by Thermo Orbitrap instruments. When IT is set to 'auto', the
instrument synchronizes IT to T_transient (Sweet Spot mode).
}
\details{
When ms2_time = "auto":
\itemize{
\item For Orbitrap analyzers: IT = T_transient (balanced/sweet spot mode)
\item This maximizes efficiency: no wasted time, no sensitivity loss
\item Example: 30K resolution -> IT = 64 ms (= T_transient)
}

When ms2_time is numeric:
\itemize{
\item Returns the value unchanged
}
}
\examples{
# Auto mode at 30K resolution -> 64 ms (T_transient)
resolve_injection_time("auto", 30000, "orbitrap")

# Auto mode at 7.5K resolution -> 16 ms
resolve_injection_time("auto", 7500, "orbitrap")

# Explicit IT value -> returned unchanged
resolve_injection_time(50, 30000, "orbitrap")  # Returns 50
}
