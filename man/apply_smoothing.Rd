% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smoothing_utils.R
\name{apply_smoothing}
\alias{apply_smoothing}
\alias{apply_smoothing.kde_config}
\alias{apply_smoothing.coverage_config}
\alias{apply_smoothing.greedy_config}
\alias{apply_smoothing.quantile_config}
\alias{apply_smoothing.outlier_config}
\alias{apply_smoothing.default}
\title{Apply Boundary Smoothing to m/z Ranges (S3 Generic)}
\usage{
apply_smoothing(
  config,
  mz_ranges,
  precursor_data,
  n_windows_per_bin = 10,
  min_width_da = 2,
  ...
)

\method{apply_smoothing}{kde_config}(
  config,
  mz_ranges,
  precursor_data,
  n_windows_per_bin = 10,
  min_width_da = 2,
  ...
)

\method{apply_smoothing}{coverage_config}(
  config,
  mz_ranges,
  precursor_data,
  n_windows_per_bin = 10,
  min_width_da = 2,
  ...
)

\method{apply_smoothing}{greedy_config}(
  config,
  mz_ranges,
  precursor_data,
  n_windows_per_bin = 10,
  min_width_da = 2,
  ...
)

\method{apply_smoothing}{quantile_config}(
  config,
  mz_ranges,
  precursor_data,
  n_windows_per_bin = 10,
  min_width_da = 2,
  ...
)

\method{apply_smoothing}{outlier_config}(
  config,
  mz_ranges,
  precursor_data,
  n_windows_per_bin = 10,
  min_width_da = 2,
  ...
)

\method{apply_smoothing}{default}(config, mz_ranges, ...)
}
\arguments{
\item{config}{A strategy_config object.}

\item{mz_ranges}{Data frame returned by \code{optimize_mz_ranges()}.}

\item{precursor_data}{Data frame for coverage recalculation after smoothing.}

\item{n_windows_per_bin}{Integer, used by greedy strategy for width constraint.}

\item{min_width_da}{Numeric, used by greedy strategy for width constraint.}

\item{...}{Additional arguments.}
}
\value{
Data frame of smoothed m/z ranges (same shape as input).
}
\description{
Post-processor invoked after \code{\link{optimize_mz_ranges}}. Dispatches
on the class of \code{config} to apply strategy-appropriate smoothing
(Whittaker-Henderson or Savitzky-Golay) to \code{mz_min} / \code{mz_max}
across RT bins, then recalculates coverage.
}
\details{
Strategies without sharp boundary semantics (kde, coverage) provide no-op
methods that return \code{mz_ranges} unchanged.
}
\keyword{internal}
