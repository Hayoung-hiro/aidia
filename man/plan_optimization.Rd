% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage2_optimization_planning.R
\name{plan_optimization}
\alias{plan_optimization}
\title{Plan DIA Window Optimization (Stage 2 Main Function)}
\usage{
plan_optimization(
  validated_data,
  current_cycle_time = NULL,
  instrument_preset = "astral",
  target_dppp = 7,
  target_satisfaction = 0.85,
  dppp_tolerance = 0,
  load_factor = 0.8,
  ms1_scans_per_cycle = NULL,
  warning_threshold_windows = 5
)
}
\arguments{
\item{validated_data}{ValidatedData object from Stage 1}

\item{current_cycle_time}{Numeric, current cycle time in seconds (default: NULL = auto-estimate)
If NULL, automatically estimated from gradient length (gradient_min / 15, max 3.5 sec)}

\item{instrument_preset}{Character, instrument type (default: "astral")
Options: "astral", "orbitrap", "exploris", "timstof", etc.}

\item{target_dppp}{Numeric, target minimum DPPP value (default: 7.0)
\itemize{
\item 7.0: Quantification mode (recommended)
\item 4.0: Balanced mode
\item 1.5: Identification mode
}}

\item{target_satisfaction}{Numeric, target satisfaction ratio 0-1 (default: 0.85)
\itemize{
\item 0.85 = 85\% of precursors meet target DPPP
}}

\item{dppp_tolerance}{Numeric, tolerance for DPPP matching (default: 0.0)}

\item{load_factor}{Numeric, scan rate utilization 0-1 (default: 0.8)
\itemize{
\item 0.8: Conservative (recommended for stability)
\item 0.9: Moderate
\item 1.0: Aggressive (may cause instability)
}}

\item{ms1_scans_per_cycle}{Integer, MS1 scans per duty cycle (default: NULL = auto-detect)
\itemize{
\item NULL: Auto-detect from instrument (parallel=0, sequential=1)
\item 0: Parallel instruments (Astral, TimsTOF) - MS1 acquired during MS2
\item 1: Sequential instruments (Orbitrap) - MS1 acquired before MS2
}}

\item{warning_threshold_windows}{Integer, low window count warning threshold (default: 5)
\itemize{
\item Issues warning if window count falls below this threshold
}}
}
\value{
OptimizationPlan S3 object
}
\description{
Integrates DPPP diagnosis and window count determination into a unified
planning stage. This function analyzes current DPPP status, calculates
required cycle time, and determines feasible window count.
}
\examples{
# Basic usage
plan <- plan_optimization(
  validated_data,
  current_cycle_time = 3.5,
  instrument_preset = "astral"
)

# Custom parameters
plan <- plan_optimization(
  validated_data,
  current_cycle_time = 2.0,
  instrument_preset = "orbitrap",
  target_dppp = 4.0,  # Balanced mode
  target_satisfaction = 0.90,  # Stricter
  load_factor = 0.9  # More aggressive
)
}
