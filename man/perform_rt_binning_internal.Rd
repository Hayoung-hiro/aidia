% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stage3_rt_binning.R
\name{perform_rt_binning_internal}
\alias{perform_rt_binning_internal}
\title{Perform RT Binning (Internal Dispatcher)}
\usage{
perform_rt_binning_internal(
  precursor_data,
  rt_bin_width_min,
  rt_binning_mode = "fixed",
  rt_column = NULL,
  cpd_significance_level = 0.05,
  cpd_min_bin_width = 1,
  cpd_max_bin_width = 15,
  cpd_min_precursors_per_bin = 50,
  edge_void_buffer_min = 0.5,
  edge_wash_min_precursors = 30
)
}
\arguments{
\item{precursor_data}{Data frame with RT.Apex column (computed in Stage 1)}

\item{rt_bin_width_min}{Numeric, RT bin width in minutes (used by fixed mode)}

\item{rt_binning_mode}{Character, "fixed" (default) or "adaptive"}

\item{rt_column}{Character, which RT column to use. NULL = auto-detect via get_rt_column()}

\item{cpd_significance_level}{Numeric, KS-test p-value threshold (adaptive only, default 0.05)}

\item{cpd_min_bin_width}{Numeric, minimum bin width in minutes (adaptive only, default 1.0)}

\item{cpd_max_bin_width}{Numeric, maximum bin width in minutes (adaptive only, default 15.0)}

\item{cpd_min_precursors_per_bin}{Integer, minimum precursors per bin (adaptive only, default 50)}

\item{edge_void_buffer_min}{Numeric, void-volume buffer in minutes (default 0.5)}

\item{edge_wash_min_precursors}{Integer, merge last bin if fewer than this (default 30)}
}
\value{
List containing:
\itemize{
\item data: precursor_data with rt_group column added
\item stats: RT statistics per bin (rt_group, rt_start, rt_end, n_precursors, rt_segment_id)
\item n_bins: Number of bins created
\item rt_breaks: RT breakpoints
\item adaptive_info: NULL for fixed mode; list of KS diagnostics for adaptive mode
}
}
\description{
Routes to fixed or adaptive RT binning based on \code{rt_binning_mode}.
All new parameters have defaults, so existing callers that only pass
\code{precursor_data} and \code{rt_bin_width_min} continue to work.
}
\keyword{internal}
