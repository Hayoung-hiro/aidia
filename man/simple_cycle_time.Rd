% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cycle_time.R
\name{simple_cycle_time}
\alias{simple_cycle_time}
\title{Compute Cycle Time from Pre-Computed MS1 / Total MS2 Times}
\usage{
simple_cycle_time(ms1_time, total_ms2_time, cycle_mode)
}
\arguments{
\item{ms1_time}{Numeric, MS1 scan time (any consistent unit; sec or ms).}

\item{total_ms2_time}{Numeric, total MS2 time (same unit as \code{ms1_time}).
Typically \code{n_windows * single_ms2_scan_time}.}

\item{cycle_mode}{Character, either \code{"parallel"} or \code{"sequential"}.}
}
\value{
Numeric, cycle time in the same unit as the inputs.
}
\description{
Lightweight alternative to \code{calculate_cycle_time_from_experiment()}
for call sites that already have MS1 scan time and total MS2 time in hand
(e.g., inside IT optimization loops). Dispatches on cycle mode:
\itemize{
\item \code{"parallel"}: \code{max(ms1_time, total_ms2_time)}
\item \code{"sequential"}: \code{ms1_time + total_ms2_time}
}
}
\details{
Use this instead of inlining \code{max(...)} or \code{+} so the cycle time
formula has a single canonical implementation in this file.
}
\examples{
\dontrun{
# Astral (parallel) at 51 windows
simple_cycle_time(ms1_time = 0.266, total_ms2_time = 0.255, "parallel")

# Exploris (sequential)
simple_cycle_time(ms1_time = 0.122, total_ms2_time = 1.6, "sequential")
}
}
\keyword{internal}
