% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/column_selection.R
\name{select_essential_columns}
\alias{select_essential_columns}
\title{Select Essential Columns (Automatic, No Configuration)}
\usage{
select_essential_columns(data, verbose = TRUE)
}
\arguments{
\item{data}{Tibble, the validated data to filter}

\item{verbose}{Logical, print column selection summary? (default: TRUE)}
}
\value{
Tibble with selected columns only
}
\description{
Automatically filters ValidatedData to keep only columns needed for
the optimization pipeline. This function:
\enumerate{
\item Always keeps ESSENTIAL_COLUMNS
\item Keeps QC_COLUMNS if they exist (after consensus)
\item Removes all other columns for memory efficiency
}
}
\details{
No user configuration needed - this always applies the same logic.
}
\examples{
\dontrun{
# Standard usage (automatic)
filtered_data <- select_essential_columns(validated_data$data)

# Silent mode
filtered_data <- select_essential_columns(validated_data$data, verbose = FALSE)
}
}
