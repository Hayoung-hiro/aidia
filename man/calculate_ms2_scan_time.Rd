% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/instrument_utils.R
\name{calculate_ms2_scan_time}
\alias{calculate_ms2_scan_time}
\title{Calculate MS2 Scan Time}
\usage{
calculate_ms2_scan_time(
  resolution = 30000,
  injection_time_ms,
  overhead_ms = NULL,
  analyzer = "orbitrap",
  verbose = TRUE
)
}
\arguments{
\item{resolution}{Numeric, Orbitrap resolution (e.g., 30000)}

\item{injection_time_ms}{Numeric, injection time in milliseconds}

\item{overhead_ms}{Numeric, overhead in ms (NULL for auto-calculate from transient)}

\item{analyzer}{Character, analyzer type (default: "orbitrap")}

\item{verbose}{Logical, print efficiency warnings (default: TRUE)}
}
\value{
List with scan time breakdown:
\itemize{
\item t_scan_ms: Total scan time in milliseconds
\item transient_ms: Transient time from resolution
\item injection_time_ms: Input IT
\item overhead_ms: Calculated or provided overhead
\item limiting_factor: "resolution", "sensitivity", "balanced", or "parallel"
\item sweet_spot_it_ms: Recommended IT for optimal efficiency
\item efficiency_pct: Efficiency percentage (100\% = optimal)
\item efficiency_mode: "auto" (optimal) or "custom" (user-defined)
\item efficiency_message: Human-readable efficiency status
}
}
\description{
Calculates the total time for a single MS2 scan based on the fundamental
scan time equation: t_scan = max(T_transient, IT) + delta
}
\details{
This is the core formula for accurate window count calculation:
\itemize{
\item Resolution determines T_transient (detection time floor)
\item IT can be shorter (Resolution-Limited) or longer (Sensitivity-Limited)
\item Overhead (delta) is always added
}
\subsection{Efficiency Modes (Orbitrap)}{

\strong{Auto Mode (IT = T_transient)}: 100\% efficiency (Resolution Limited)
\itemize{
\item Optimal balance between speed and sensitivity
\item No wasted scan time
}

\strong{Custom Mode (IT > T_transient)}: Reduced efficiency (Injection Limited)
\itemize{
\item Longer cycle time, fewer windows per cycle
\item DPPP may decrease
\item Use for low-abundance samples requiring more sensitivity
}
}
}
\examples{
\dontrun{
calculate_ms2_scan_time(30000, 50)   # 30K, 50ms IT -> 76.8 ms
calculate_ms2_scan_time(30000, 80)   # 30K, 80ms IT -> 92.8 ms
calculate_ms2_scan_time(80000, 3.0, analyzer = "astral")  # Astral
}
}
\keyword{internal}
