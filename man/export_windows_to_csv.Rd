% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/export_methods.R
\name{export_windows_to_csv}
\alias{export_windows_to_csv}
\title{Export Windows to CSV for Instrument Upload}
\usage{
export_windows_to_csv(
  optimized_windows,
  output_file,
  validated_data,
  charge_state = 1L,
  acquisition_start_min = 0,
  acquisition_end_min = NULL
)
}
\arguments{
\item{optimized_windows}{OptimizedWindows object}

\item{output_file}{Character, output CSV file path}

\item{validated_data}{ValidatedData object (for N_Precursors calculation)}

\item{charge_state}{Integer, value for the \code{z} column (default: 1). DIA wide
windows isolate by m/z range, so charge is metadata only and does not affect
acquisition. Default is 1 (the Xcalibur default) because Xcalibur's mass-list
importer flags \code{z = 0} as invalid and drops it, forcing manual re-entry.
Set to 0 to request "ignore charge state" if your importer accepts it.}

\item{acquisition_start_min}{Numeric, run start in minutes (default: 0). Sets
the first segment's \verb{t start}, closing the leading MS1-only void.}

\item{acquisition_end_min}{Numeric or NULL (default: NULL). LC method total
length in minutes; sets the last segment's \verb{t stop}, closing the trailing
void. NULL warns and falls back to the last segment's measured \code{rt_end}.}
}
\value{
NULL (invisible), writes CSV file
}
\description{
Creates an instrument-ready CSV file in the 8-column Thermo Xcalibur Targeted
Mass List format, compatible with Thermo Orbitrap instruments.
}
