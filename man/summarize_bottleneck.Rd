% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capacity_kpis.R
\name{summarize_bottleneck}
\alias{summarize_bottleneck}
\title{Summarize the Dominant Capacity Bottleneck}
\usage{
summarize_bottleneck(kpis, thresholds = capacity_kpi_thresholds())
}
\arguments{
\item{kpis}{List returned by \code{\link[=get_capacity_kpis]{get_capacity_kpis()}}.}

\item{thresholds}{List from \code{\link[=capacity_kpi_thresholds]{capacity_kpi_thresholds()}}. Currently used
for forward-compatible signature symmetry with \code{\link[=get_capacity_kpis]{get_capacity_kpis()}}.}
}
\value{
Character scalar -- the message text (English).
}
\description{
Returns a single English sentence describing the dominant capacity
condition. Rules are evaluated in priority order; the first matching
rule wins. See \code{docs/domain-knowledge.md} "Bottleneck Summary -- Rule
Set" for the full table.
}
\section{Rule order}{

\enumerate{
\item DPPP \code{Bad} (equivalently cycle \code{Bad}) -- target not met.
\item Filled ratio \code{Bad} (skipped when \code{filled_window_ratio} is \code{NA}).
\item Filled ratio \code{Warn} with no other \code{Bad}
(skipped when \code{filled_window_ratio} is \code{NA}).
\item Cycle \code{OK} and window \code{OK} and DPPP \code{Info}, filled not confirmed
\code{OK} -- ceiling with DPPP slack, IT / m/z tradeoff available.
\item Cycle \code{Info} and window \code{Info} -- underutilized.
\item DPPP \code{Info} and cycle, window, filled all \code{OK} -- DPPP-only slack;
lengthen IT.
\item All \code{OK} -- well balanced.
\item Fallback message.
}
}

