% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/smoothing_utils.R
\name{smooth_whittaker}
\alias{smooth_whittaker}
\title{Whittaker-Henderson Smoothing with Per-Point Weights}
\usage{
smooth_whittaker(y, weights = NULL, lambda = 10, d = 2)
}
\arguments{
\item{y}{Numeric vector to smooth}

\item{weights}{Numeric vector of per-point weights (default: uniform).
Higher weight = trust this point more. Use e.g. sqrt(n_precursors).}

\item{lambda}{Smoothing parameter (default: 10). Higher = smoother.
Typical range: 1 (light) to 1000 (very heavy).}

\item{d}{Difference order for penalty (default: 2 for second-order).}
}
\value{
Smoothed numeric vector (same length as y)
}
\description{
Penalized least squares smoother that balances data fidelity against
smoothness. Supports per-point weights for adaptive smoothing:
high-weight points are preserved, low-weight points are smoothed
toward their neighbors.
}
\details{
Based on Eilers (2003) "A Perfect Smoother". No external dependencies.
}
\keyword{internal}
