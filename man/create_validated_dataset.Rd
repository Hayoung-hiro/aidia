% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_validation.R
\name{create_validated_dataset}
\alias{create_validated_dataset}
\title{Validate DIA-NN Data and Create ValidatedData Structure}
\usage{
create_validated_dataset(
  proteome_file,
  raw_file_dir = NULL,
  rt_range = NULL,
  mz_range = NULL,
  enable_raw_metadata = FALSE,
  enable_replicate_consensus = TRUE,
  min_replicates = 1,
  max_intensity_cv_percent = 30,
  quality_threshold = 0.8,
  apply_quality_filters = TRUE,
  ...
)
}
\arguments{
\item{proteome_file}{Path to DIA-NN output file (.parquet, .tsv, .csv)}

\item{raw_file_dir}{Path to raw files directory (optional, for metadata extraction)}

\item{rt_range}{RT filtering range c(min, max) in minutes (optional)}

\item{mz_range}{m/z filtering range c(min, max) in Da (optional)}

\item{enable_raw_metadata}{Whether to attempt raw file metadata extraction (default: FALSE)}

\item{enable_replicate_consensus}{Enable technical replicate consensus handling (default: TRUE)}

\item{min_replicates}{Minimum number of replicates to keep (default: 1)}

\item{max_intensity_cv_percent}{Maximum intensity CV\% threshold for filtering (default: 30)}

\item{quality_threshold}{Minimum quality score 0-1 (default: 0.8)}

\item{apply_quality_filters}{Apply DIA-NN Q-value filters (default: TRUE)}

\item{...}{Additional arguments passed to filter_diann_quality()}
}
\value{
ValidatedData S3 object with data, metadata, and validation_status
}
\description{
Main function for Stage 1: Loads DIA-NN output, performs quality validation,
and creates a structured ValidatedData object for downstream stages.
}
\details{
Pipeline Pattern: Data flows through transformation functions:
Load -> Validate Columns -> Handle Replicates -> Select Columns -> Validate Quality -> Package

Column Selection: Automatically keeps only essential columns for memory efficiency
(Precursor.Id, RT.Start, Precursor.Mz, FWHM, Protein.Group + QC columns if present)
}
