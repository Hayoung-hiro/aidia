% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_window_gantt.R
\name{plot_isolation_window_gantt}
\alias{plot_isolation_window_gantt}
\title{Isolation Window Gantt Chart}
\usage{
plot_isolation_window_gantt(
  optimized_windows,
  validated_data = NULL,
  show_precursors = FALSE,
  max_precursors = 5000
)
}
\arguments{
\item{optimized_windows}{OptimizedWindows object containing windows tibble with columns:
rt_segment_id, rt_start, rt_end, window_id, mz_start, mz_end, mz_center, mz_width, n_precursors}

\item{validated_data}{ValidatedData object (optional). If provided and show_precursors = TRUE,
overlays actual precursor locations}

\item{show_precursors}{Logical. If TRUE and validated_data is provided, overlay precursor points}

\item{max_precursors}{Integer. Maximum precursors to plot (randomly sampled if exceeded).
Prevents rendering issues with large datasets. Default: 5000}
}
\value{
A ggplot object showing isolation windows as rectangles in RT x m/z space
}
\description{
Creates a Gantt-style chart showing all isolation windows as rectangles in RT x m/z space.
Each window is colored by precursor density. Optional precursor overlay available.
}
\details{
The plot shows:
\itemize{
\item X-axis: Retention Time (minutes)
\item Y-axis: m/z (Da)
\item Rectangles: Isolation windows (rt_start to rt_end x mz_start to mz_end)
\item Fill color: n_precursors (viridis cividis scale, colorblind safe)
\item RT bin boundaries: Vertical dashed lines
\item Optional precursor overlay: Scatter points showing actual precursor locations
}
}
\examples{
\dontrun{
# Basic Gantt chart
p1 <- plot_isolation_window_gantt(optimized_windows)

# With precursor overlay
p2 <- plot_isolation_window_gantt(optimized_windows, validated_data,
                                   show_precursors = TRUE)
}

}
\keyword{internal}
