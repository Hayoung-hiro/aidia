% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/capacity_kpis.R
\name{classify_capacity_kpi}
\alias{classify_capacity_kpi}
\title{Classify a Capacity KPI Value}
\usage{
classify_capacity_kpi(value, kpi_name, thresholds)
}
\arguments{
\item{value}{Numeric scalar (the KPI value to classify).}

\item{kpi_name}{Character, one of \code{"filled_window_ratio"},
\code{"dppp_headroom_x"}, \code{"cycle_time_headroom_pct"},
\code{"window_count_headroom_pct"}.}

\item{thresholds}{List from \code{\link[=capacity_kpi_thresholds]{capacity_kpi_thresholds()}}.}
}
\value{
Character scalar: \code{"Bad"}, \code{"Warn"}, \code{"OK"}, \code{"Info"}, or \code{"NA"}.
}
\description{
Maps a numeric KPI value to one of \code{"Bad"}, \code{"Warn"}, \code{"OK"}, \code{"Info"},
or \code{"NA"} using the thresholds returned by \code{\link[=capacity_kpi_thresholds]{capacity_kpi_thresholds()}}.
Two-sided KPIs (DPPP / cycle / window headroom) use \code{Bad / OK / Info};
the monotonic \code{filled_window_ratio} uses \code{Bad / Warn / OK}.
}
\details{
Boundary rule: lower bound is inclusive, upper bound exclusive
(\verb{Bad < bad <= Warn < warn <= OK < info <= Info}). The
\code{filled_window_ratio} value of exactly \code{0.70} is \code{Warn}; exactly \code{0.90}
is \code{OK}.
}
