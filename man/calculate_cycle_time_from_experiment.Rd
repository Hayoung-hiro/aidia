% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cycle_time.R
\name{calculate_cycle_time_from_experiment}
\alias{calculate_cycle_time_from_experiment}
\title{Calculate Actual Cycle Time from User Experiment Config}
\usage{
calculate_cycle_time_from_experiment(
  experiment_config,
  verbose = TRUE,
  language = "ko"
)
}
\arguments{
\item{experiment_config}{List containing experiment parameters (from YAML or Shiny input)}

\item{verbose}{Logical, print detailed breakdown (default: TRUE)}

\item{language}{Character, output language: "ko" (Korean) or "en" (English)}
}
\value{
List with calculated cycle time and detailed breakdown
}
\description{
Calculates the precise cycle time based on actual experimental parameters
rather than using heuristic estimates. This provides accurate DPPP calculations.
}
\details{
The cycle time calculation uses the fundamental scan time equations:

\strong{For Orbitrap:}
\itemize{
\item t_scan = max(T_transient, IT) + overhead
\item T_transient is determined by resolution
}

\strong{For Astral:}
\itemize{
\item t_scan = max(5.0 ms, IT + 2.0 ms) for parallel architecture
}

\strong{Cycle Time:}
\itemize{
\item Sequential: cycle_time = MS1_time + (n_windows x MS2_time)
\item Parallel: cycle_time = max(MS1_time, n_windows x MS2_time)
}
}
\examples{
\dontrun{
# From YAML config
config <- yaml::read_yaml("config/user_experiment_config.yaml")
result <- calculate_cycle_time_from_experiment(config)

# From direct parameters
config <- list(
  instrument = list(preset = "exploris"),
  ms1 = list(resolution = 60000, max_injection_time_ms = 50),
  ms2 = list(resolution = 15000, max_injection_time_ms = "auto"),
  dia_windows = list(window_count = 40)
)
result <- calculate_cycle_time_from_experiment(config)
cat(sprintf("Cycle time: \%.3f sec\\\\n", result$cycle_time_sec))
}
}
\keyword{internal}
